@*@Ignore*@
@{
    @using SmartNQuick.AspMvc.Modules.View
    @model SmartNQuick.AspMvc.Models.Persistence.Test.EditForm

    var viewBagInfo = new ViewBagWrapper(ViewBag);

    if (viewBagInfo.DisplayProperty.Name.Equals("CheckBox"))
    {
        viewBagInfo.Handled = true;
        <input type="checkbox" asp-for="CheckBox" />
    }
}