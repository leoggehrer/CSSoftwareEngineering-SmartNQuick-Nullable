@*@BaseCode*@
@*This partial view can alternatively be used with the shared index view.*@
@*#if ACCOUNT_ON*@
@{
    @using SmartNQuick.AspMvc.Modules.Language
    @using SmartNQuick.AspMvc.Modules.Session
    @using SmartNQuick.AspMvc.Modules.View
    @model SmartNQuick.AspMvc.Models.Business.Account.IdentityUser

    var viewBagWrapper = new ViewBagWrapper(ViewBag);
    var sessionWrapper = new SessionWrapper(Context.Session);

    viewBagWrapper.Translate = Translator.TranslateIt;
    viewBagWrapper.Controller = ViewContext.RouteData.Values["controller"].ToString();
    viewBagWrapper.Action = ViewContext.RouteData.Values["action"].ToString();

    <tr>
        <td>
            @Html.DisplayFor(modelItem => Model.OneItem.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.OneItem.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.AnotherItem.Lastname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.AnotherItem.Firstname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.OneItem.EnableJwtAuth)
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.OneItem.AccessFailedCount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.OneItem.State)
        </td>
        <td>
            @if ((viewBagWrapper.CommandMode & SmartNQuick.AspMvc.Models.Modules.Common.CommandMode.Edit) > 0)
            {
                <a type="button" href="@($"/{viewBagWrapper.Controller}/EDit/{Model.Id}")" class="btn btn-outline-primary btn-sm" title="@viewBagWrapper.TranslateFor("Edit")"><i class="fa fa-edit" aria-hidden="true"></i></a>
            }
            @if ((viewBagWrapper.CommandMode & SmartNQuick.AspMvc.Models.Modules.Common.CommandMode.ShowDetails) > 0)
            {
                <a type="button" href="@($"/{viewBagWrapper.Controller}/Details/{Model.Id}")" class="btn btn-outline-secondary btn-sm" title="@viewBagWrapper.TranslateFor("Details")"><i class="fa fa-list" aria-hidden="true"></i></a>
            }
            @if ((viewBagWrapper.CommandMode & SmartNQuick.AspMvc.Models.Modules.Common.CommandMode.Remove) > 0)
            {
                <a type="button" href="@($"/{viewBagWrapper.Controller}/Delete/{Model.Id}")" class="btn btn-outline-danger btn-sm" title="@viewBagWrapper.TranslateFor("Delete")"><i class="fa fa-trash" aria-hidden="true"></i></a>
            }
        </td>
    </tr>
}
@*#endif*@
